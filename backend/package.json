{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "build:docker": "docker build --provenance=false --platform linux/amd64 -t bun-lambda-demo:latest .",
    "start:lambda": "bun run src/index.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.100.0",
    "@types/node": "^25.5.2"
  }
}